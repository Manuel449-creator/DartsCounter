import SwiftUI

struct CompletedTournamentHeader: View {
    let tournament: Tournament
    
    var body: some View {
        // ... (bisheriger Code)
    }
}

struct ActiveTournamentHeader: View {
    let tournament: Tournament
    
    // ... (bisheriger Code)
}